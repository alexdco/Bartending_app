import { AccountPageClient } from "@/auth/account-page-client";

export default function AccountPage() {
  return (
    <main className="flex flex-1 flex-col">
      <AccountPageClient />
    </main>
  );
}
