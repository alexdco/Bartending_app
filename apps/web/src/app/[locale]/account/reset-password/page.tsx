import { ResetPasswordPageClient } from "@/auth/reset-password-page-client";

export default function ResetPasswordPage() {
  return (
    <main className="flex flex-1 flex-col">
      <ResetPasswordPageClient />
    </main>
  );
}
